# =============================================================================
# Next-Gen PlanFact — зависимости Python
# Зафиксированы минимальные версии; точные версии фиксируются через pip-compile.
# =============================================================================

# --- Web-фреймворк ---
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0        # standard = uvloop + httptools + websockets

# --- База данных ---
sqlalchemy[asyncio]>=2.0.30,<3.0.0
asyncpg>=0.29.0,<1.0.0                   # async PostgreSQL драйвер
alembic>=1.13.0,<2.0.0                   # миграции схемы БД

# --- Pydantic v2 ---
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0         # Settings из env-переменных

# --- Google Gemini AI ---
google-genai>=1.0.0,<2.0.0              # официальный Google AI unified SDK

# --- Безопасность / Аутентификация ---
bcrypt>=4.1.0,<5.0.0                     # хэширование паролей
python-jose[cryptography]>=3.3.0,<4.0.0 # JWT (будущая реализация get_current_user)
passlib[bcrypt]>=1.7.4,<2.0.0

# --- HTTP-клиент (для банковских интеграций и внешних вызовов) ---
httpx>=0.27.0,<1.0.0

# --- Утилиты ---
python-dotenv>=1.0.0,<2.0.0             # загрузка .env файла
python-multipart>=0.0.9,<1.0.0          # form-data / файловые загрузки
orjson>=3.10.0,<4.0.0                   # быстрая JSON-сериализация (FastAPI подхватывает автоматически)

# --- Логирование / Мониторинг ---
structlog>=24.1.0,<25.0.0               # структурированное логирование (опционально)
sentry-sdk[fastapi]>=2.0.0,<3.0.0      # мониторинг ошибок (опционально)
aiosqlite>=0.20.0,<1.0.0               # SQLite async (fallback для локальной разработки)
